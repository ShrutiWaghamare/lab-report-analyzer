# Lab Report Analyzer — Requirements
# ─────────────────────────────────────────────────────────────
# CORE (always required)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.0

# Rule-based ML (local, no external API)
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0
nltk>=3.8.1
textstat>=0.7.3
joblib>=1.3.0

# ─────────────────────────────────────────────────────────────
# OCR STACK
# ─────────────────────────────────────────────────────────────
pytesseract>=0.3.10          # Python wrapper for Tesseract
Pillow>=10.0.0               # Image I/O + fallback preprocessing

# OpenCV — main image preprocessing engine
# Use headless (no GUI) for server environments
opencv-python-headless>=4.9.0

# PDF handling
pdf2image>=1.17.0            # Render PDF pages to images (needs: apt install poppler-utils)
pymupdf>=1.24.0              # Fast text-layer PDF extraction (no system deps needed)

# System dependencies (install separately):
#   sudo apt-get install tesseract-ocr poppler-utils
#   For Hindi/other languages: sudo apt-get install tesseract-ocr-hin

# ─────────────────────────────────────────────────────────────
# AI AGENT / LLM  (optional — only for ExplanationAgent)
# ─────────────────────────────────────────────────────────────
# Option A: CrewAI framework
# crewai>=0.51.0

# Option B: Azure OpenAI
# langchain>=0.2.0
# langchain-openai>=0.1.0

# Option C: Anthropic Claude
# langchain-anthropic>=0.1.0

# ─────────────────────────────────────────────────────────────
# ASYNC TASK QUEUE (production background processing)
# celery[redis]>=5.3.0
# redis>=5.0.0
